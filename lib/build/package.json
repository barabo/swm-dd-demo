{
  "name": "swm-client-lib",
  "version": "0.1.16",
  "description": "",
  "main": "index.html",
  "exports": {
    ".": {
      "browser": {
        "default": "./swm.js"
      }
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "snowpack dev --port 3333",
    "build": "snowpack build",
    "prepublish": "bash prepublish.sh",
    "publish": "cd ./build 2>/dev/null && npm publish || echo PUBLISHED",
    "format": "prettier --write swm.js",
    "lint": "prettier --check swm.js"
  },
  "author": "Carl Anderson (carl.anderson@microsoft.com)",
  "license": "MIT",
  "dependencies": {
    "uuid": "8.3.2"
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "snowpack": "^3.3.7"
  }
}
